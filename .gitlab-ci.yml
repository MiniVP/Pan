image: sglahn/platformio-core
stages:
  - test

variables:
  PLATFORMIO_HOME_DIR: "$CI_PROJECT_DIR/.platformio"

cache:
  paths:
    - .platformio

before_script:
  - update

pan-ht:
  stage: test
  script:
    - run -d Pan_HT

pan-l:
  stage: test
  script:
    - run -d Pan_L

pan-uv:
  stage: test
  script:
    - run -d Pan_UV
